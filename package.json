{
  "name": "daily-games",
  "version": "1.0.0",
  "description": "A new micro game every day — dashboard + game collection",
  "private": true,
  "scripts": {
    "test": "bun test",
    "test:dashboard": "bun test dashboard/test/",
    "test:e2e": "npx playwright test",
    "test:e2e:smoke": "npx playwright test smoke",
    "test:e2e:multiplayer": "npx playwright test sync-or-sink",
    "start": "bun dashboard/server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
